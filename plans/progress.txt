Ralph Iteration - Feature: navbar-001 (Persistent Top Navbar) + chat-001 (Chat Sidebar)

Date: 2026-02-15

What was implemented:
- Created persistent navbar at front/src/components/layout/navbar.tsx
  - Page title/breadcrumb based on current route
  - User profile dropdown with profile, settings, logout options
  - Notification bell with unread indicator
  - Search bar for quick navigation
  - Alti button to open chat sidebar
  - Responsive design with mobile menu
- Created chat sidebar at front/src/components/chat/chat-sidebar.tsx
  - Right-side slide-in panel using Sheet-like overlay approach
  - Chat interface with message input
  - Full-width toggle (bonus feature)
  - Smooth slide animation
  - Close on click outside or Escape key
- Updated root layout.tsx to integrate navbar and chat sidebar

Technical decisions:
- Used direct Tailwind CSS (shadcn/ui components not installed in project)
- Used Next.js useSession for auth state
- Chat sidebar implemented with fixed positioning + transform for slide animation
- Created handleKeyPress for Enter-to-send functionality

Status: COMPLETED - Navbar and chat sidebar created and integrated

Next iteration: chat-002 (Chat View Mode Toggle) - but chat-001 already includes full-width toggle, so this may be redundant

Ralph Iteration - Feature: layout-001 (Conditional Layout Rendering for Auth Pages)

Date: 2026-02-16

What was implemented:
- Created Next.js route groups for clean separation of auth and app layouts
- Created src/app/(auth)/layout.tsx - minimal layout without sidebar/navbar
- Created src/app/(app)/layout.tsx - full app layout with sidebar, navbar, chat sidebar
- Updated root layout.tsx to be minimal (just html/body wrapper)
- Moved auth pages from /auth/* to (auth) route group:
  - /auth/signin -> /signin
  - /auth/signup -> /signup
  - /auth/accept-invitation -> /accept-invitation
- Moved all app pages to (app) route group:
  - /dashboard, /projects, /tasks, /teams, /organizations, /processes, etc.
- Removed obsolete app-shell.tsx component (functionality moved to (app)/layout.tsx)

Technical decisions:
- Used Next.js route groups (parentheses syntax) as recommended in PRD
- Route groups don't affect URL structure - auth pages are now at /signin, /signup (no /auth prefix)
- App layout contains SessionProvider, Sidebar, Navbar, and ChatSidebar
- Auth layout is minimal - just gray background with centered content
- This approach avoids conditional rendering based on pathname and provides cleaner separation

Files changed:
- front/src/app/layout.tsx - simplified to minimal root layout
- front/src/app/(auth)/layout.tsx - new auth layout
- front/src/app/(app)/layout.tsx - new app layout (migrated from app-shell.tsx)
- Removed: front/src/components/layout/app-shell.tsx
- Moved all auth pages from front/src/app/auth/* to front/src/app/(auth)/*
- Moved all app pages to front/src/app/(app)/*

Status: COMPLETED - Route groups implemented, auth pages no longer show sidebar/navbar

Next iteration: navbar-002 (Merge Duplicate Navigation Bars) or auth-001 (Sign In Page Implementation)

Ralph Iteration - Feature: auth-001 (Sign In Page Implementation)

Date: 2026-02-16

What was implemented:
- Enhanced auth.ts with Google and Microsoft OAuth providers
  - Added GoogleProvider and MicrosoftProvider (azure-ad) with environment variable checks
  - Updated signIn callback to auto-create users from OAuth providers
  - Fixed signIn path from /auth/signin to /signin for route groups
- Completely redesigned signin page at front/src/app/(auth)/signin/page.tsx:
  - Modern card-based UI with white card on gray background
  - Email input with validation (regex check for valid email format)
  - Password field with show/hide toggle
  - Remember me checkbox (persists email to localStorage)
  - Forgot password link (points to /forgot-password)
  - Loading states with Loader2 spinner
  - OAuth buttons for Google and Microsoft (with SVG icons)
  - Error handling for OAuth errors (OAuthAccountNotLinked)
  - Success messages for invitation acceptance flow
  - Responsive design with proper spacing
- Fixed signup page links to use /signin instead of /auth/signin

Technical decisions:
- OAuth providers only enabled when environment variables are set (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, AZURE_AD_CLIENT_ID, AZURE_AD_CLIENT_SECRET)
- OAuth auto-creates users if they don't exist (similar to passwordless signup)
- Remember me stores email in localStorage (not password for security)
- OAuthAccountNotLinked error handled with user-friendly message

Files changed:
- front/src/lib/auth.ts - Added OAuth providers and signIn callback
- front/src/app/(auth)/signin/page.tsx - Complete redesign
- front/src/app/(auth)/signup/page.tsx - Fixed route links

Status: COMPLETED - Sign in page fully implemented with email/password and OAuth providers

Next iteration: auth-002 (Sign Up Page Implementation) or auth-003 (Authentication Context and Token Management)

Ralph Iteration - Feature: auth-002 (Sign Up Page Implementation)

Date: 2026-02-16

What was implemented:
- Enhanced register API route at front/src/app/api/auth/register/route.ts:
  - Added password strength validation (8 chars, uppercase, lowercase, number, special char)
  - Added firstName/lastName support (combines to fullName)
  - Added acceptTerms validation
  - Returns specific error messages for password requirements
- Completely redesigned signup page at front/src/app/(auth)/signup/page.tsx:
  - First name and last name fields with user icon
  - Email field with validation and error display
  - Password field with show/hide toggle
  - Password strength indicator (Weak/Fair/Good/Strong/Very Strong)
  - Visual checklist for password requirements (checkmarks when met)
  - Confirm password field with match validation
  - Terms and conditions checkbox with links to /terms and /privacy
  - OAuth buttons for Google and Microsoft (matching signin page style)
  - Loading states with Loader2 spinner
  - Success message after registration
  - Auto sign-in after successful registration
  - Responsive design matching signin page

Technical decisions:
- Password requirements validated on both client and server side
- Password strength shows visual progress bar with color coding
- confirmPassword field shows inline error when passwords don't match
- Form submit disabled until all requirements met and terms accepted
- OAuth sign-up uses same flow as sign-in (auto-creates user if not exists)
- Used Suspense boundary for useSearchParams hook

Files changed:
- front/src/app/api/auth/register/route.ts - Enhanced with password validation
- front/src/app/(auth)/signup/page.tsx - Complete redesign

Status: COMPLETED - Sign up page fully implemented with password strength indicator, terms acceptance, and OAuth providers

Next iteration: auth-003 (Authentication Context and Token Management) or navbar-002 (Merge Duplicate Navigation Bars)
