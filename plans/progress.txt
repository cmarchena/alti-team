Ralph Iteration - Feature: navbar-001 (Persistent Top Navbar) + chat-001 (Chat Sidebar)

Date: 2026-02-15

What was implemented:
- Created persistent navbar at front/src/components/layout/navbar.tsx
  - Page title/breadcrumb based on current route
  - User profile dropdown with profile, settings, logout options
  - Notification bell with unread indicator
  - Search bar for quick navigation
  - Alti button to open chat sidebar
  - Responsive design with mobile menu
- Created chat sidebar at front/src/components/chat/chat-sidebar.tsx
  - Right-side slide-in panel using Sheet-like overlay approach
  - Chat interface with message input
  - Full-width toggle (bonus feature)
  - Smooth slide animation
  - Close on click outside or Escape key
- Updated root layout.tsx to integrate navbar and chat sidebar

Technical decisions:
- Used direct Tailwind CSS (shadcn/ui components not installed in project)
- Used Next.js useSession for auth state
- Chat sidebar implemented with fixed positioning + transform for slide animation
- Created handleKeyPress for Enter-to-send functionality

Status: COMPLETED - Navbar and chat sidebar created and integrated

Next iteration: chat-002 (Chat View Mode Toggle) - but chat-001 already includes full-width toggle, so this may be redundant

Ralph Iteration - Feature: layout-001 (Conditional Layout Rendering for Auth Pages)

Date: 2026-02-16

What was implemented:
- Created Next.js route groups for clean separation of auth and app layouts
- Created src/app/(auth)/layout.tsx - minimal layout without sidebar/navbar
- Created src/app/(app)/layout.tsx - full app layout with sidebar, navbar, chat sidebar
- Updated root layout.tsx to be minimal (just html/body wrapper)
- Moved auth pages from /auth/* to (auth) route group:
  - /auth/signin -> /signin
  - /auth/signup -> /signup
  - /auth/accept-invitation -> /accept-invitation
- Moved all app pages to (app) route group:
  - /dashboard, /projects, /tasks, /teams, /organizations, /processes, etc.
- Removed obsolete app-shell.tsx component (functionality moved to (app)/layout.tsx)

Technical decisions:
- Used Next.js route groups (parentheses syntax) as recommended in PRD
- Route groups don't affect URL structure - auth pages are now at /signin, /signup (no /auth prefix)
- App layout contains SessionProvider, Sidebar, Navbar, and ChatSidebar
- Auth layout is minimal - just gray background with centered content
- This approach avoids conditional rendering based on pathname and provides cleaner separation

Files changed:
- front/src/app/layout.tsx - simplified to minimal root layout
- front/src/app/(auth)/layout.tsx - new auth layout
- front/src/app/(app)/layout.tsx - new app layout (migrated from app-shell.tsx)
- Removed: front/src/components/layout/app-shell.tsx
- Moved all auth pages from front/src/app/auth/* to front/src/app/(auth)/*
- Moved all app pages to front/src/app/(app)/*

Status: COMPLETED - Route groups implemented, auth pages no longer show sidebar/navbar

Next iteration: navbar-002 (Merge Duplicate Navigation Bars) or auth-001 (Sign In Page Implementation)

Ralph Iteration - Feature: auth-001 (Sign In Page Implementation)

Date: 2026-02-16

What was implemented:
- Enhanced auth.ts with Google and Microsoft OAuth providers
  - Added GoogleProvider and MicrosoftProvider (azure-ad) with environment variable checks
  - Updated signIn callback to auto-create users from OAuth providers
  - Fixed signIn path from /auth/signin to /signin for route groups
- Completely redesigned signin page at front/src/app/(auth)/signin/page.tsx:
  - Modern card-based UI with white card on gray background
  - Email input with validation (regex check for valid email format)
  - Password field with show/hide toggle
  - Remember me checkbox (persists email to localStorage)
  - Forgot password link (points to /forgot-password)
  - Loading states with Loader2 spinner
  - OAuth buttons for Google and Microsoft (with SVG icons)
  - Error handling for OAuth errors (OAuthAccountNotLinked)
  - Success messages for invitation acceptance flow
  - Responsive design with proper spacing
- Fixed signup page links to use /signin instead of /auth/signin

Technical decisions:
- OAuth providers only enabled when environment variables are set (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, AZURE_AD_CLIENT_ID, AZURE_AD_CLIENT_SECRET)
- OAuth auto-creates users if they don't exist (similar to passwordless signup)
- Remember me stores email in localStorage (not password for security)
- OAuthAccountNotLinked error handled with user-friendly message

Files changed:
- front/src/lib/auth.ts - Added OAuth providers and signIn callback
- front/src/app/(auth)/signin/page.tsx - Complete redesign
- front/src/app/(auth)/signup/page.tsx - Fixed route links

Status: COMPLETED - Sign in page fully implemented with email/password and OAuth providers

Next iteration: auth-002 (Sign Up Page Implementation) or auth-003 (Authentication Context and Token Management)

Ralph Iteration - Feature: auth-002 (Sign Up Page Implementation)

Date: 2026-02-16

What was implemented:
- Enhanced register API route at front/src/app/api/auth/register/route.ts:
  - Added password strength validation (8 chars, uppercase, lowercase, number, special char)
  - Added firstName/lastName support (combines to fullName)
  - Added acceptTerms validation
  - Returns specific error messages for password requirements
- Completely redesigned signup page at front/src/app/(auth)/signup/page.tsx:
  - First name and last name fields with user icon
  - Email field with validation and error display
  - Password field with show/hide toggle
  - Password strength indicator (Weak/Fair/Good/Strong/Very Strong)
  - Visual checklist for password requirements (checkmarks when met)
  - Confirm password field with match validation
  - Terms and conditions checkbox with links to /terms and /privacy
  - OAuth buttons for Google and Microsoft (matching signin page style)
  - Loading states with Loader2 spinner
  - Success message after registration
  - Auto sign-in after successful registration
  - Responsive design matching signin page

Technical decisions:
- Password requirements validated on both client and server side
- Password strength shows visual progress bar with color coding
- confirmPassword field shows inline error when passwords don't match
- Form submit disabled until all requirements met and terms accepted
- OAuth sign-up uses same flow as sign-in (auto-creates user if not exists)
- Used Suspense boundary for useSearchParams hook

Files changed:
- front/src/app/api/auth/register/route.ts - Enhanced with password validation
- front/src/app/(auth)/signup/page.tsx - Complete redesign

Status: COMPLETED - Sign up page fully implemented with password strength indicator, terms acceptance, and OAuth providers

Next iteration: auth-003 (Authentication Context and Token Management) or navbar-002 (Merge Duplicate Navigation Bars)

Ralph Iteration - Feature: auth-003 (Authentication Context and Token Management)

Date: 2026-02-16

What was implemented:
- Created AuthContext at front/src/contexts/AuthContext.tsx:
  - AuthProvider component wrapping the app
  - useAuth hook for accessing auth state
  - RequireAuth component for protecting routes
  - RedirectIfAuthenticated component for auth pages
  - Global auth state: user, isAuthenticated, isLoading
  - Methods: login(), loginWithOAuth(), logout(), refreshUser()
  - Automatic redirect for unauthenticated users on protected routes
  - Session persistence via NextAuth session
- Created /api/auth/me endpoint at front/src/app/api/auth/me/route.ts:
  - Returns current authenticated user data
  - Uses getServerSession for server-side auth check
- Created /api/auth/logout endpoint at front/src/app/api/auth/logout/route.ts:
  - Clears NextAuth session cookies
- Updated SessionProvider.tsx to wrap AuthProvider inside NextAuthSessionProvider
- Updated navbar.tsx to use useAuth hook instead of useSession
- Updated middleware.ts matcher to exclude all public routes

Technical decisions:
- Used NextAuth session as the source of truth for auth state (JWT strategy)
- AuthProvider fetches additional user data from /api/auth/me endpoint
- Protected routes handled by middleware (server-side) + RequireAuth (client-side)
- PUBLIC_ROUTES array defines routes accessible without authentication
- CallbackUrl support for redirecting back after login
- The logout endpoint clears both secure and non-secure cookie variants for cross-environment support

Files changed:
- front/src/contexts/AuthContext.tsx - New auth context and hooks
- front/src/app/api/auth/me/route.ts - New endpoint for fetching current user
- front/src/app/api/auth/logout/route.ts - New endpoint for logout
- front/src/app/components/SessionProvider.tsx - Added AuthProvider wrapper
- front/src/components/layout/navbar.tsx - Use useAuth hook
- front/src/middleware.ts - Updated matcher for public routes

Status: COMPLETED - Auth context provides global authentication state, protected routes redirect unauthenticated users

Next iteration: navbar-002 (Merge Duplicate Navigation Bars) or route-001 (Departments Page Implementation)

Ralph Iteration - Feature: navbar-002 (Merge Duplicate Navigation Bars)

Date: 2026-02-16

What was implemented:
- Created PageHeaderContext at front/src/contexts/PageHeaderContext.tsx:
  - PageHeaderProvider component for managing page header state
  - usePageHeader hook for accessing header state
  - useSetPageHeader hook for pages to set their header content declaratively
  - Supports: title, breadcrumbs, and action buttons (ReactNode)
- Updated (app)/layout.tsx to wrap children with PageHeaderProvider
- Refactored navbar.tsx to:
  - Read title, breadcrumbs, actions from PageHeaderContext
  - Display breadcrumbs when provided (with ChevronRight separators)
  - Fall back to title-based display when no breadcrumbs
  - Render page-specific action buttons in the right section
  - Action buttons visible in both desktop and mobile views
- Removed duplicate headers from all main pages:
  - projects/page.tsx - Removed separate <header>, added "New Project" button to navbar
  - tasks/page.tsx - Removed separate header, added conditional "New Task" button to navbar
  - teams/page.tsx - Removed separate <header>, integrated with navbar
  - dashboard/page.tsx - Removed separate header, added "Sign out" button to navbar
  - organizations/page.tsx - Removed separate <header>, added "New Organization" button to navbar
  - processes/page.tsx - Removed separate <header>, added "Create Ad-hoc" and "Use Template" buttons to navbar

Technical decisions:
- Used React Context pattern for page header state management
- Actions are ReactNode type allowing flexible button compositions
- Each page uses useSetPageHeader hook to set its header content on mount
- Cleanup effect in useSetPageHeader clears header state on unmount
- Page titles now show only once (in navbar) - no duplication
- Action buttons render conditionally (e.g., Tasks page only shows "New Task" when a project is selected)

Files changed:
- front/src/contexts/PageHeaderContext.tsx - New context for page header management
- front/src/app/(app)/layout.tsx - Added PageHeaderProvider wrapper
- front/src/components/layout/navbar.tsx - Refactored to use PageHeaderContext
- front/src/app/(app)/projects/page.tsx - Removed duplicate header, use page header context
- front/src/app/(app)/tasks/page.tsx - Removed duplicate header, use page header context
- front/src/app/(app)/teams/page.tsx - Removed duplicate header, use page header context
- front/src/app/(app)/dashboard/page.tsx - Removed duplicate header, use page header context
- front/src/app/(app)/organizations/page.tsx - Removed duplicate header, use page header context
- front/src/app/(app)/processes/page.tsx - Removed duplicate header, use page header context

Status: COMPLETED - Single unified navbar across all pages with page-specific action buttons integrated

Acceptance criteria met:
- Only one navigation bar visible across all pages ✓
- Page-specific action buttons render in the navbar right section ✓
- All global navigation elements remain functional ✓
- No duplicate page titles ✓
- Responsive design maintained ✓
- All existing pages work with merged navbar ✓

Next iteration: route-001 (Departments Page Implementation) or route-002 (Resources Page Implementation)

Ralph Iteration - Feature: route-001 (Departments Page Implementation)

Date: 2026-02-16

What was implemented:
- Created departments page at front/src/app/(app)/departments/page.tsx:
  - Department listing in table format with search and filtering
  - Organization selector dropdown (switches between user's organizations)
  - Search bar for filtering departments by name
  - "New Department" button in navbar action slot
  - Create department modal with name and description fields
  - Edit department modal for updating name and description
  - Delete department modal with confirmation and warning about members
  - Member count badge for each department
  - Empty state handling when no organizations exist
  - Loading states for all async operations
  - Error handling with inline error messages
- Enhanced departments API at front/src/app/api/departments/route.ts:
  - Added member count to each department (_count.members)
  - Changed access control to allow both owners and members to view departments
  - Added getTeamMemberRepository import for fetching member data

Technical decisions:
- Used table layout similar to teams page for consistency
- Organization dropdown lets user switch context between their organizations
- Search query filters departments client-side for responsiveness
- Member count calculated server-side by counting team members with matching departmentId
- Empty state prompts user to create an organization if they have none
- Access control now allows organization members (not just owners) to view departments
- Only organization owners can create/edit/delete departments (enforced by existing API)

Files changed:
- front/src/app/(app)/departments/page.tsx - New departments page
- front/src/app/api/departments/route.ts - Enhanced with member counts and updated access control

Acceptance criteria met:
- Page renders at /departments route ✓
- No 404 error when navigating to departments ✓
- Departments list displays correctly ✓
- Search and filter functionality works ✓
- Can create new department ✓
- Can edit existing department ✓
- Can delete department with confirmation ✓
- Responsive design on all devices ✓
- Integrates with merged navbar ✓

Status: COMPLETED - Departments page fully implemented with CRUD operations

Next iteration: route-002 (Resources Page Implementation)
